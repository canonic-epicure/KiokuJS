StartTest(function(t) {
    
    var async0 = t.beginAsync()
    
    use('KiokuJS', function () {
        
        //======================================================================================================================================================================================================================================================
        t.diag('Sanity')
        
        t.ok(KiokuJS, "KiokuJS is here")
        
        t.done()
        
        t.endAsync(async0)
    })
})    